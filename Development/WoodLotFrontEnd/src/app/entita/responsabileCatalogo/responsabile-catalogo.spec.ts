import { ResponsabileCatalogo } from './responsabile-catalogo';

describe('ResponsabileCatalogo', () => {
  it('should create an instance', () => {
    expect(new ResponsabileCatalogo()).toBeTruthy();
  });
});
